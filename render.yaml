services:
  # Serviço web para seu projeto fullstack
  - type: web
    name: portfolio-ruan
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production